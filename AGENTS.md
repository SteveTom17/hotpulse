# 行为宪章：热点内容作战台

> 本文件是 HotPulse 的最高优先级产品与工程约束。任何功能、增长实验、客户要求或模型输出与本宪章冲突时，都必须停止并进入人工复核；不得以收入目标为理由降低来源、事实、标识、授权或审批标准。

## 1. 使命与边界

本项目帮助经过授权的品牌、商家和内容团队发现值得响应的公共热点，产出**可编辑、可核查、需人工确认后才能发布**的内容方案。它不是自动化灌水、舆情操纵、绕过平台限制或替代专业审核的工具。

默认产品范围：热点信号采集、趋势研判、品牌相关性评估、内容草案生成、素材导出、审核留痕与效果回收。

明确不做：

- 绕过登录、验证码、访问限制或反爬机制的数据抓取；
- 未经平台授权的批量发布、代登录、模拟用户操作或“自动养号”；
- 伪造新闻、观点、评论、互动量、用户身份或“官方”背书；
- 基于真实人物声音或肖像生成未获授权的内容；
- 生成违法、侵权、仇恨、骚扰、色情、暴力、歧视、欺诈或有害虚假信息。

## 2. 数据与平台行为

1. 只接入平台官方 API、获得书面授权的供应商，或由客户主动导入且有权使用的数据。
2. 每个热点都必须显示来源、采集时间、许可/接口状态与原始链接；无法追溯来源的信号不得标为“已验证热点”。
3. 遵守数据源的使用条款、配额、缓存期限和删除要求。接口不可用时，降级为“数据暂不可用”，不以爬虫绕过。
4. 不存储平台账号密码、Cookie 或访问令牌明文。令牌最小权限、加密保存、可撤销，并记录调用主体。
5. 仅收集达成产品目的所需的数据；客户数据按租户隔离，并支持导出和删除。
6. 正式模型服务（当前为 DeepSeek/OpenAI 兼容网关）接入前必须完成内容安全、数据处理与输出标识评估；模型输出只是草案，不得伪装成事实；模型故障时降级为“生成暂不可用”或显著标注的演示引擎，不静默替换。
7. 连接器凭据（密钥/令牌）以 AES-256-GCM 加密保存，接口只读展示状态；同步失败必须记录原因并按指数退避暂停重试，不制造重试风暴。

## 3. 内容生成规则

1. 生成内容必须标记为“AI 草案”，且不得自动对外发布。
2. 对图片、音频、视频等生成或合成素材，按适用规则添加显式提示和可追溯的元数据标识；导出时不得剥离标识。
3. 对新闻、公共事件、医疗、金融、法律、未成年人、灾害、事故、政治及争议性议题，默认提高风险等级；未给出可靠来源时，只能输出“待核实”分析，不得写成事实。
4. 禁止仿冒特定在世人物、机构或品牌的官方口吻、声音或形象；引用第三方作品须由用户确认拥有权利或使用许可。
5. 带货文案不得虚构价格、库存、功效、资质、奖项、用户评价或促销时效。所有商品事实必须来自用户确认的商品资料。
6. 避免把悲剧、灾害、伤害、隐私泄露或群体冲突转为营销机会；风险较高时建议放弃借势。

## 4. 人工审核与发布

1. 任何对外发布都必须由具备发布权限的人工用户明确确认；系统仅可提供“复制”“导出”或在已授权渠道中发起待发布草稿。
2. 发布前展示：数据来源、生成时间、风险提示、涉及的声明/商品事实、AI 标识状态与最终文案。
3. 记录生成、编辑、审批、导出和发布动作的审计日志。审批人与发布人可为同一人，但必须可追溯。
4. 发现严重侵权、违法、有害或明显失实内容时，停止生成/导出，保留必要审计记录，并提供申诉与人工复核入口。
5. 用户行使数据删除权时，删除其可识别数据；依法必需的审计留痕与协议同意记录保留（审计事件解除工作区关联，不随删除消失），并向用户明示保留范围。

## 5. 质量与透明度

1. 明确区分“数据事实”“模型推断”“创意建议”；不得把热度分、预测分或情绪判断伪装成事实。
2. 热点排序必须说明至少包含哪些维度（如增速、跨源出现、行业相关性、风险），并展示更新时间。
3. 对模型不确定、数据不足或时效过期的结果，主动降低置信度并提示用户复核。
4. 无障碍、隐私、安全与合规优先于转化率和生成数量。

## 6. 工程工作约定

1. 修改前先识别数据来源、权限与风险等级；涉及新增外部数据源、发布能力或敏感行业规则，必须更新本文件与 `requirements.md`。
2. 以最小可行、可观测、可回滚为原则。新增接口应有鉴权、限流、错误处理、审计日志和测试。
3. 不在代码、日志、示例或文档中提交密钥、个人信息、客户内容或平台凭据。
4. 所有用户可见的“热点”结论和商品事实应能定位到输入或来源；测试数据须标为模拟数据。
5. 计费与额度必须透明：每次扣减写入用量明细，人工开通/支付确认须留审计；不得以虚构价格、虚假稀缺、夸大功效或隐藏条款促成销售。额度耗尽时拒绝生成并提示升级，不静默降级为低质量产物。
