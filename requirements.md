# 功能需求与验收标准

## 1. 产品定义

**产品名（暂定）**：热点内容作战台

**目标用户**：美妆、餐饮、本地生活等单一垂直行业的品牌运营、商家、MCN 和代运营团队。

**首版目标**：用户每天在 15 分钟内完成“发现一个相关热点 → 判断是否值得跟进 → 生成并审核一套内容草案 → 导出待发布素材”。首版不承诺自动发布，不承诺实时全网热搜，也不以未经授权的数据抓取为前提。

**首版建议行业**：本地生活/餐饮。理由是热点切入与促销、门店活动和短视频脚本的关联清晰，商品或服务事实能由商家后台提供并核验。

### 1.1 当前实现状态（0.1.0）

**已实现（服务端持久化 + 多租户授权）**：

- 工作区、品牌资料与成员角色（管理员/编辑/审批人/只读成员）及角色化权限门禁；两个工作区的热点、资料、内容与日志完全隔离（FR-01）。
- 数据连接器层：授权 HTTP API 连接器（凭据加密保存、指数退避、限流暂停、审计留痕）与客户 CSV 导入（显著标注“客户导入”）；24 小时窗口语义去重、跨源归并并保留全部原始链接（FR-02/FR-03）；Cron 每 30 分钟定时同步。
- 规则优先风险分级（低/中/高/禁止）与生成阻断；审批人可将低/中风险上调并留下原因，不能强行导出禁止项（FR-04）。
- 正式模型网关：DeepSeek（OpenAI 兼容）接入、结构化提示词、输出安全校验、`[待确认]` 占位符提取、额度扣减；未配置密钥时自动降级为显著标注的演示引擎（FR-05）。
- 内容包：生成、编辑、恢复历史版本、版本哈希；三确认人工审批（来源/事实/AI 标识）；合规导出包 manifest 含来源清单、事实清单、AI 标识与版本历史（FR-05/FR-07）。
- 审计日志：按内容包与动作类型筛选、分页；生成/编辑/审批/导出/风险覆盖/连接器失败均可追溯；普通编辑无删除权限（FR-07）。
- 计费试点：免费试用 30 天/30 额度，管理员人工开通专业版（¥399/月/工作区，150 额度/月），用量明细与已支付发票留痕；额度耗尽拒绝生成并提示升级。
- 法律与隐私：用户协议/隐私政策同意留痕（版本可追溯）、申诉/投诉/数据请求工单、账户数据删除（删除后审计留痕依法保留）。

**尚未宣称完成**：平台官方实时热搜接入、真实支付网关、自动发布、效果回收报表、企业级 SSO 与精细保留策略。

当前所有演示热点、来源和品牌事实必须显著标注为模拟数据，不得直接对外发布；正式模型接入前必须先完成来源、内容安全、数据处理与标识评估（见 README 上线前清单）。

## 2. 实现思路与范围

```text
官方/授权趋势数据 + 客户导入素材
             │
             ▼
       标准化、去重、来源留存
             │
             ▼
  热点评分（增速 × 跨源 × 行业相关性 − 风险）
             │
             ▼
   热点卡片 + 证据 + 风险提示 + 跟进建议
             │
             ▼
  LLM 生成内容包（脚本/文案/表情包创意）
             │
             ▼
  事实校验、AI 标识、人工审批、导出与审计
```

### 2.1 首版技术切分

- **数据连接器层**：先实现抖音官方热门视频榜、微博官方/授权关键词检索或榜单供应商；每个连接器独立配置凭据、配额和数据保留策略。
- **趋势处理层**：定时任务拉取、统一字段、语义去重、计算 24 小时增速与跨源信号；保存原始来源链接和采集时间。
- **决策层**：规则优先的风险分类 + LLM 辅助的行业相关性摘要；评分可解释、可人工覆盖。
- **生成层**：用结构化提示词生成多版本脚本、带货文案及图片/表情包制作提示；只在用户明确选择时调用图像或视频生成能力。
- **审核与导出层**：编辑器、必填事实确认、风险确认、AI 标识检查、导出包和不可篡改审计日志。

### 2.1.1 技术栈决定

- **应用**：TypeScript + React 19 + Vinext，兼顾产品界面、服务端接口和 Cloudflare Worker 运行环境。
- **样式**：Tailwind CSS 4 与语义化 CSS 变量；遵循 WCAG 对比度、键盘操作、44px 触控目标和减少动态效果偏好。
- **数据**：Cloudflare D1 + Drizzle ORM；浏览器状态只承载临时界面交互，不作为工作区、审批或审计记录的权威来源。
- **文件**：MVP 仅导出小型 JSON/文本清单；后续图片、视频与 ZIP 等二进制资产使用 R2，D1 只保存元数据。
- **身份**：托管版本优先使用平台提供的已认证用户信息，并在服务端执行工作区授权；不自行拼装未经评估的账号密码系统。
- **模型**：通过服务端生成接口隔离供应商；当前为可审计演示引擎，正式模型上线前必须完成来源、内容安全、数据处理与标识评估。

### 2.2 非功能基线

- 多租户隔离；角色至少包含管理员、编辑、审批人、只读成员。
- 敏感凭据不进入前端、日志或仓库；服务端加密保存并可撤销。
- 趋势数据标明“更新时间”和“数据源”；数据失败时不能展示为新鲜结果。
- 目标可用性：月度 99.5%（不包括第三方数据源或模型故障）；第三方故障需提供明确降级提示。
- 首屏热点列表在已有缓存时 P95 小于 2 秒；一次纯文本内容包生成 P95 小于 30 秒。

## 3. 功能需求与验收

### FR-01：工作区与品牌资料

**需求**：管理员创建工作区、邀请成员，并维护品牌资料：行业、目标客群、禁用话题、语气、可核验的商品/门店事实和合规声明。

**验收标准**：

1. 两个工作区相互不可见对方的热点、品牌资料、内容和日志。
2. 编辑只能编辑草案；审批人才能批准导出；管理员能管理成员与数据连接器。
3. 生成带货文案时，系统只能引用当前工作区已确认的商品事实；没有资料时显示占位符和“待确认”。

### FR-02：授权热点数据接入

**需求**：按计划任务获取至少两个经授权的数据源；保存来源、链接、采集时间、原始标题、热度字段和接口状态。

**验收标准**：

1. 每条热点可打开来源链接，并显示数据源与最近更新时间。
2. 连接器遇到权限、限流或故障时记录失败原因、停止重试风暴，并在界面显示“数据暂不可用”。
3. 系统不要求、不保存用户的平台密码或 Cookie；没有授权连接器时允许管理员导入 CSV，且标注“客户导入”。
4. 不得通过模拟浏览器、验证码绕过或未授权抓取来填补数据缺口。

### FR-03：热点归并、评分与预警

**需求**：将相似热点归并为事件卡片，并按趋势、跨源信号、行业相关性和风险计算“建议跟进度”。

**验收标准**：

1. 每张卡片展示：标题、摘要、数据源数量、最早/最近采集时间、评分构成、风险等级和证据链接。
2. 用户可按行业相关性、风险、时间范围和数据源筛选；可将热点标为“忽略”“观察”或“生成内容”。
3. 同一话题在 24 小时内来自多个源时，归并结果保留各来源，不能丢失原始链接。
4. 评分低置信或数据超过配置新鲜度时，界面不得标示“实时”或“已验证”。

### FR-04：风险判断与人工覆盖

**需求**：对热点和生成请求做分级：低、中、高、禁止。高风险议题默认不提供营销借势内容；禁止项直接阻断。

**验收标准**：

1. 涉及灾害、事故、伤害、未成年人、隐私、医疗、金融、法律、政治或争议性公共事件时，至少显示风险原因和人工核验提示。
2. 对禁止内容，系统拒绝生成并记录类别，不回显危险细节。
3. 审批人可把低/中风险调高，并留下原因；不能把系统判定为禁止的内容强行导出。
4. 风险分类和人工覆盖都写入审计日志。

### FR-05：内容包生成与编辑

**需求**：用户基于一张热点卡与品牌资料，生成可编辑的内容包，至少包含：3 个标题/钩子、1 个 30–60 秒短视频脚本、1 组图文/带货文案、1 个表情包或视觉创意说明。

**验收标准**：

1. 每个内容包关联唯一热点卡、生成模型/版本、输入的品牌资料版本、生成时间和风险状态。
2. 文案中引用的具体商品价格、功效、库存、活动时间等事实必须可链接到品牌资料；没有证据时使用 `[待确认]`，不可伪造。
3. 用户可编辑、重生成单个模块、比较版本和恢复上一版本。
4. 界面明确标注“AI 草案，发布前请核验”，且不会自动创建任何平台帖子。

### FR-06：图片/视频素材请求与 AI 标识

**需求**：当用户请求生成图片、音频或视频素材时，系统提供制作任务及导出文件，并维护内容标识状态。

**验收标准**：

1. 生成任务显示成本/额度预估、输入描述、状态和失败原因；重复提交具备幂等保护。
2. 成功素材在预览和导出包中标示 AI 生成/合成状态；导出前检查必需的显式提示和元数据标识。
3. 用户不能选择真实人物仿声、肖像仿冒或受保护角色作为无授权素材；系统改为要求上传授权证明或拒绝。
4. 导出的素材包包含 `manifest.json`，列出文件、来源/生成信息、生成时间和标识状态。

### FR-07：人工审批、导出与审计

**需求**：内容包只能在审批后导出；首版提供复制、下载 ZIP 或生成待发布清单，不提供自动外发。

**验收标准**：

1. 未审批内容的导出按钮不可用，并提示风险和待确认项。
2. 审批操作要求确认风险等级、事实校验和 AI 标识状态，并记录审批人、时间、版本哈希。
3. 导出包至少含最终文本、素材、来源清单、事实清单、AI 标识说明和版本信息。
4. 审计日志可按内容包查询生成、编辑、审批、导出与失败操作；普通编辑不能删除日志。

### FR-08：效果回收（首版轻量）

**需求**：用户手工录入或从授权渠道导入已发布内容的链接与基础效果，供团队复盘。

**验收标准**：

1. 可记录发布时间、渠道、链接、播放/互动/转化等可选指标以及人工备注。
2. 报表能按热点、内容类型和时间筛选，并明确区分“用户录入”和“授权接口数据”。
3. 系统不得依据少量样本宣称因果归因；只展示描述性比较。

## 4. MVP 里程碑

| 阶段 | 可交付物 | 退出条件 |
| --- | --- | --- |
| M0：可行性 | 单行业访谈、数据源授权确认、品牌资料模板 | 至少 5 个目标用户确认同一日常工作流与付费痛点 |
| M1：洞察 | 工作区、一个授权数据源、热点卡与手动导入 | 用户能看到来源可追溯的热点并完成筛选 |
| M2：创作 | 风险分级、内容包、编辑器、事实占位 | 用户能在 15 分钟内完成一份可审的草案 |
| M3：交付 | 审批、导出包、AI 标识、审计日志 | 未审批内容无法导出，导出内容完整可追溯 |
| M4：验证 | 轻量效果回收、试点账单与留存指标 | 3–5 个付费试点持续使用 4 周 |

## 5. 成功指标与上线门槛

**北极星指标**：每周“完成审批并导出”的有效内容包数量 / 活跃工作区。

**试点目标**：

- 3–5 家付费或明确承诺付费的试点客户；
- 每个活跃工作区每周至少导出 3 个内容包；
- 从选定热点到导出内容包的中位时间不高于 15 分钟；
- 100% 导出包具备来源清单、审批记录和 AI 标识状态；
- 0 起未经人工审批的自动发布；0 起已知的未授权数据抓取。

**不得上线的条件**：数据源无授权路径、导出无法保留 AI 标识、无法审计审批动作、或高风险内容可被轻易绕过。

## 6. 后续范围（不属于 MVP）

- 经平台书面授权后的“提交待发布草稿”集成；仍保持人工最终确认。
- 更多垂直行业的规则包和商品目录连接器。
- 经授权的实时趋势、竞品监测和自动化排期。
- 企业级 SSO、精细保留策略、法务审批工作流与私有部署。
